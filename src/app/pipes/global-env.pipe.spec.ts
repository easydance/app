import { GlobalEnvPipe } from './global-env.pipe';

describe('GlobalEnvPipe', () => {
  it('create an instance', () => {
    const pipe = new GlobalEnvPipe();
    expect(pipe).toBeTruthy();
  });
});
