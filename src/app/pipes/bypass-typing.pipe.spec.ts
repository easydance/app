import { BypassTypingPipe } from './bypass-typing.pipe';

describe('BypassTypingPipe', () => {
  it('create an instance', () => {
    const pipe = new BypassTypingPipe();
    expect(pipe).toBeTruthy();
  });
});
